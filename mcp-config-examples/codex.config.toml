# Add this block to ~/.codex/config.toml (or your project-level config).
# Check OpenAI's current Codex docs for the exact key names before the demo —
# this schema moves faster than the others.

[mcp_servers.buildtutor]
command = "node"
args = ["/absolute/path/to/buildtutor-mcp/build/index.js"]

[mcp_servers.buildtutor.env]
ANTHROPIC_API_KEY = "sk-ant-your-key-here"
